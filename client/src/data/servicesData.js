// Re-export complete 14-service dataset from fallbackData for backward compatibility
import { FALLBACK_SERVICES, FALLBACK_FAQS } from './fallbackData';

export const servicesData = FALLBACK_SERVICES;
export const faqsData = FALLBACK_FAQS;
